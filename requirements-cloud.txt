# Lo mínimo para la ejecución en GitHub Actions: sin interfaz gráfica ni
# servidor web, solo buscar, guardar en Postgres y escribir el informe.
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.3
PyYAML>=6.0
psycopg[binary]>=3.2
